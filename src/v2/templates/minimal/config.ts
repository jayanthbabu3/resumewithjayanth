/**
 * Minimal Template Configuration
 * 
 * Re-exports the existing config from the config directory.
 */

export { minimalConfig as config } from '../../config/templates/minimal';
export { minimalConfig } from '../../config/templates/minimal';
