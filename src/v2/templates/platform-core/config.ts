/**
 * Platform Core Template Configuration
 *
 * Re-exports the config from the config directory.
 */

export { platformCoreConfig as config } from '../../config/templates/platform-core';
export { platformCoreConfig } from '../../config/templates/platform-core';
