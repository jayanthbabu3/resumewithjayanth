/**
 * API Ledger Template Configuration
 *
 * Re-exports the config from the config directory.
 */

export { apiLedgerConfig as config } from '../../config/templates/api-ledger';
export { apiLedgerConfig } from '../../config/templates/api-ledger';
