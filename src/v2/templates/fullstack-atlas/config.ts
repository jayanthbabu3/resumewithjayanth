/**
 * Fullstack Atlas Template Configuration
 *
 * Re-exports the config from the config directory.
 */

export { fullstackAtlasConfig as config } from '../../config/templates/fullstack-atlas';
export { fullstackAtlasConfig } from '../../config/templates/fullstack-atlas';
