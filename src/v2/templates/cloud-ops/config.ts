/**
 * Cloud Ops Template Configuration
 *
 * Re-exports the config from the config directory.
 */

export { cloudOpsConfig as config } from '../../config/templates/cloud-ops';
export { cloudOpsConfig } from '../../config/templates/cloud-ops';
