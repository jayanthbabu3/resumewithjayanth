/**
 * Refined Portrait Template Configuration
 */

import { refinedPortraitConfig } from '@/v2/config/templates/refined-portrait';

export const config = refinedPortraitConfig;

export default config;
