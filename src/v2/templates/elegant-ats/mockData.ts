/**
 * Elegant ATS Template Mock Data
 *
 * Uses the sample V2 resume data to showcase the layout.
 */

import type { V2ResumeData } from '@/v2/types';
import { SAMPLE_V2_RESUME_DATA } from '@/v2/data/sampleData';

export const mockData: V2ResumeData = SAMPLE_V2_RESUME_DATA;

export default mockData;
