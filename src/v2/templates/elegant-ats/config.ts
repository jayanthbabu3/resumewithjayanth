/**
 * Elegant ATS Template Configuration
 *
 * Re-exports the config from the shared templates config folder
 * so template consumers can import locally.
 */

export { elegantAtsConfig as config } from '../../config/templates/elegant-ats';
export { elegantAtsConfig } from '../../config/templates/elegant-ats';
