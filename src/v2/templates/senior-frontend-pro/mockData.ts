/**
 * Senior Frontend Pro Template Mock Data
 * 
 * Sample resume data optimized for the senior frontend pro layout.
 * Features an experienced Senior Frontend Developer.
 */

import type { ResumeData } from '@/types/resume';
import { SENIOR_FRONTEND_DATA } from '@/v2/data/mockData';

export const mockData: ResumeData = SENIOR_FRONTEND_DATA;

export default mockData;
