/**
 * Executive Split Template Configuration
 * 
 * Re-exports the existing config from the config directory.
 */

export { executiveSplitConfig as config } from '../../config/templates/executive-split';
export { executiveSplitConfig } from '../../config/templates/executive-split';
